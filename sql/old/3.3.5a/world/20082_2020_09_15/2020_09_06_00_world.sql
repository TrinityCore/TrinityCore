UPDATE `trinity_string` SET `content_default`='You can only summon a player to your instance if that player, or their group leader, is in your instance.' WHERE `entry`=103;
UPDATE `trinity_string` SET `content_default`='You can only teleport to a player\'s instance if you are in their group, or you have GM mode enabled.' WHERE `entry`=105;
